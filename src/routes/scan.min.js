const express=require("express"),cors=require("cors"),scanner=require("../services/scanner"),zipBuilder=require("../utils/zipBuilder"),config=require("../config"),logger=require("../logger"),router=express.Router();function parseBoolean(value){return!0===value||"true"===value||"1"===value}function normalizeScanOptions(source){return{url:source.url,depth:void 0!==source.depth?Number(source.depth):0,concurrency:void 0!==source.concurrency?Number(source.concurrency):6,timeoutMs:void 0!==source.timeoutMs?Number(source.timeoutMs):config.REQUEST_TIMEOUT_MS,maxPages:void 0!==source.maxPages?Number(source.maxPages):20,allowExternal:parseBoolean(source.allowExternal)}}router.options("/scan",cors()),router.get("/scan",async(req,res)=>{try{const options=normalizeScanOptions(req.query);if(!options.url)return res.status(400).json({error:"URL is required",status:400});if(!Number.isInteger(options.depth)||options.depth<0||options.depth>5)return res.status(400).json({error:"Depth must be an integer between 0 and 5",status:400});if(!Number.isInteger(options.concurrency)||options.concurrency<1||options.concurrency>20)return res.status(400).json({error:"Concurrency must be an integer between 1 and 20",status:400});if(!Number.isInteger(options.maxPages)||options.maxPages<1)return res.status(400).json({error:"Max pages must be a positive integer",status:400});const actualMaxPages=Math.min(options.maxPages,config.HARD_MAX_PAGES);logger.info(`Scan request: url=${options.url}, depth=${options.depth}, maxPages=${actualMaxPages}`);const result=await scanner.scanWebsite({url:options.url,depth:options.depth,concurrency:options.concurrency,timeoutMs:options.timeoutMs,maxPages:actualMaxPages,allowExternal:options.allowExternal,downloadImages:!1});res.json({scannedPages:result.scannedPages,images:result.images.map(img=>({url:img.url,alt:img.alt,tag:img.tag,pageUrls:img.pageUrls,contentLength:img.contentLength,contentType:img.contentType,width:img.width,height:img.height,status:img.status})),stats:result.stats})}catch(err){logger.error("Error in GET /api/scan:",err),res.status(500).json({error:err.message||"Internal server error",status:500})}}),router.post("/scan",async(req,res)=>{try{const{url:url,depth:depth=0,concurrency:concurrency=6,timeoutMs:timeoutMs=config.REQUEST_TIMEOUT_MS,maxPages:maxPages=20,allowExternal:allowExternal=!1}=req.body;if(!url)return res.status(400).json({error:"URL is required",status:400});if(!Number.isInteger(depth)||depth<0||depth>5)return res.status(400).json({error:"Depth must be an integer between 0 and 5",status:400});if(!Number.isInteger(concurrency)||concurrency<1||concurrency>20)return res.status(400).json({error:"Concurrency must be an integer between 1 and 20",status:400});if(!Number.isInteger(maxPages)||maxPages<1)return res.status(400).json({error:"Max pages must be a positive integer",status:400});const actualMaxPages=Math.min(maxPages,config.HARD_MAX_PAGES);logger.info(`Scan request: url=${url}, depth=${depth}, maxPages=${actualMaxPages}`);const result=await scanner.scanWebsite({url:url,depth:depth,concurrency:concurrency,timeoutMs:timeoutMs,maxPages:actualMaxPages,allowExternal:allowExternal,downloadImages:!1});res.json({scannedPages:result.scannedPages,images:result.images.map(img=>({url:img.url,alt:img.alt,tag:img.tag,pageUrls:img.pageUrls,contentLength:img.contentLength,contentType:img.contentType,width:img.width,height:img.height,status:img.status})),stats:result.stats})}catch(err){logger.error("Error in /api/scan:",err),res.status(500).json({error:err.message||"Internal server error",status:500})}});const scanJobs=require("../services/scanJobs");router.post("/scan/async",(req,res)=>{const options={url:req.body.url,depth:void 0!==req.body.depth?Number(req.body.depth):0,concurrency:void 0!==req.body.concurrency?Number(req.body.concurrency):6,timeoutMs:void 0!==req.body.timeoutMs?Number(req.body.timeoutMs):config.REQUEST_TIMEOUT_MS,maxPages:void 0!==req.body.maxPages?Number(req.body.maxPages):20,allowExternal:parseBoolean(req.body.allowExternal)};if(!options.url)return res.status(400).json({error:"URL is required",status:400});const jobId=scanJobs.createJob(options);res.status(202).json({jobId:jobId,statusUrl:`/api/scan/jobs/${jobId}`,resultUrl:`/api/scan/jobs/${jobId}/result`})}),router.get("/scan/jobs/:id",(req,res)=>{const job=scanJobs.getJob(req.params.id);if(!job)return res.status(404).json({error:"Job not found",status:404});const meta={id:job.id,status:job.status,createdAt:job.createdAt,startedAt:job.startedAt,completedAt:job.completedAt,error:job.error,stats:job.result?job.result.stats:void 0};res.json(meta)}),router.get("/scan/jobs/:id/result",(req,res)=>{const job=scanJobs.getJob(req.params.id);if(!job)return res.status(404).json({error:"Job not found",status:404});"completed"===job.status?res.json(job.result):"failed"===job.status?res.status(500).json({error:job.error||"Job failed",status:500}):res.status(202).json({status:job.status,message:"Job not completed yet"})}),router.all("/scan",(req,res)=>{res.set("Allow","GET,POST,OPTIONS"),res.status(405).json({error:`Method ${req.method} not allowed on /api/scan. Use POST.`,status:405})}),router.options("/scan/zip",cors()),router.get("/scan/zip",async(req,res)=>{try{const options=normalizeScanOptions(req.query),includeImages=void 0===req.query.includeImages||parseBoolean(req.query.includeImages);if(!options.url)return res.status(400).json({error:"URL is required",status:400});if(!Number.isInteger(options.depth)||options.depth<0||options.depth>5)return res.status(400).json({error:"Depth must be an integer between 0 and 5",status:400});if(!Number.isInteger(options.concurrency)||options.concurrency<1||options.concurrency>20)return res.status(400).json({error:"Concurrency must be an integer between 1 and 20",status:400});if(!Number.isInteger(options.maxPages)||options.maxPages<1)return res.status(400).json({error:"Max pages must be a positive integer",status:400});const actualMaxPages=Math.min(options.maxPages,config.HARD_MAX_PAGES);logger.info(`Scan & ZIP request: url=${options.url}, depth=${options.depth}, includeImages=${includeImages}`);const result=await scanner.scanWebsite({url:options.url,depth:options.depth,concurrency:options.concurrency,timeoutMs:options.timeoutMs,maxPages:actualMaxPages,allowExternal:options.allowExternal,downloadImages:includeImages}),zipBuffer=await zipBuilder.buildZipStream(result.images.map(img=>({url:img.url,alt:img.alt,tag:img.tag,pageUrls:img.pageUrls,contentLength:img.contentLength,contentType:img.contentType,width:img.width,height:img.height,status:img.status,buffer:img.buffer})),includeImages);res.setHeader("Content-Type","application/zip"),res.setHeader("Content-Disposition",'attachment; filename="image-scan-results.zip"'),res.setHeader("Content-Length",zipBuffer.length),res.send(zipBuffer)}catch(err){logger.error("Error in GET /api/scan/zip:",err),res.status(500).json({error:err.message||"Internal server error",status:500})}}),router.all("/scan/zip",(req,res)=>{res.set("Allow","GET,POST,OPTIONS"),res.status(405).json({error:`Method ${req.method} not allowed on /api/scan/zip. Use POST.`,status:405})}),router.post("/scan/zip",async(req,res)=>{try{const{url:url,depth:depth=0,concurrency:concurrency=6,timeoutMs:timeoutMs=config.REQUEST_TIMEOUT_MS,maxPages:maxPages=20,allowExternal:allowExternal=!1,includeImages:includeImages=!1}=req.body;if(!url)return res.status(400).json({error:"URL is required",status:400});if(!Number.isInteger(depth)||depth<0||depth>5)return res.status(400).json({error:"Depth must be an integer between 0 and 5",status:400});if(!Number.isInteger(concurrency)||concurrency<1||concurrency>20)return res.status(400).json({error:"Concurrency must be an integer between 1 and 20",status:400});if(!Number.isInteger(maxPages)||maxPages<1)return res.status(400).json({error:"Max pages must be a positive integer",status:400});const actualMaxPages=Math.min(maxPages,config.HARD_MAX_PAGES);logger.info(`Scan & ZIP request: url=${url}, depth=${depth}, includeImages=${includeImages}`);const result=await scanner.scanWebsite({url:url,depth:depth,concurrency:concurrency,timeoutMs:timeoutMs,maxPages:actualMaxPages,allowExternal:allowExternal,downloadImages:includeImages}),zipBuffer=await zipBuilder.buildZipStream(result.images.map(img=>({url:img.url,alt:img.alt,tag:img.tag,pageUrls:img.pageUrls,contentLength:img.contentLength,contentType:img.contentType,width:img.width,height:img.height,status:img.status,buffer:img.buffer})),includeImages);res.setHeader("Content-Type","application/zip"),res.setHeader("Content-Disposition",'attachment; filename="image-scan-results.zip"'),res.setHeader("Content-Length",zipBuffer.length),res.send(zipBuffer)}catch(err){logger.error("Error in /api/scan/zip:",err),res.status(500).json({error:err.message||"Internal server error",status:500})}}),router.get("/config",(req,res)=>{res.json({hardMaxPages:config.HARD_MAX_PAGES,maxConcurrency:20,minConcurrency:1,maxDepth:5})}),module.exports=router;